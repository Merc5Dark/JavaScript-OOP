// Importing the Circle class from the 'circle.js' module
import { Circle } from './circle.js';

// Creating a new Circle instance with radius 11
const c = new Circle(11);

// Calling the 'draw' method on the Circle instance to display the circle's information
c.draw();

// Output:
// Circle with radius 11
